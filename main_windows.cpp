// this file is only used as an entry-point for the compiler to typecheck the mystd;
// for some reason MSVC refuses to accept the header file by itself, so that's why we need this file

#include "include_windows.h"

int main() { return 0; }
